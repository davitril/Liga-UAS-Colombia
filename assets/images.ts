// assets/images.ts

// Using a simple 1x1 transparent gif as a placeholder to fix the build error.
const placeholderImage = 'data:image/gif;base64,R0lGODlhAQABAIAAAAAAAP///yH5BAEAAAAALAAAAAABAAEAAAIBRAA7';

// Good result images (Score >= 71%)
export const happyMalePilotImage = placeholderImage;
export const happyFemalePilotImage = placeholderImage;

// Normal result images (Score >= 50%)
export const normalMalePilotImage = placeholderImage;
export const normalFemalePilotImage = placeholderImage;

// Bad result images (Score < 50%)
export const sadMalePilotImage = placeholderImage;
export const sadFemalePilotImage = placeholderImage;
